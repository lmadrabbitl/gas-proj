info:
  name: Update
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:8080/accounts/ssavinsgss
  body:
    type: json
    data: |-
      { 
        "name": "Updated ssssssss",
        "type": "ASSET",
        "currency": "ABC"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
