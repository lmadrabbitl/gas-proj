info:
  name: Get All Account Balances
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/reports/balance
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
