info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/auth/login
  body:
    type: json
    data: |-
      { 
      "email": "test@user.com", 
      "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
