info:
  name: Create test user
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/auth/create_test_user
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
